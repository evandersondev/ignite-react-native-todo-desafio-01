import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  divisor: {
    height: 1,
    backgroundColor: '#333333',
  },
  clipboard: {
    marginLeft: 'auto',
    marginRight: 'auto',
    marginBottom: 16,
    marginTop: 48,
  },
  emptyListText: {
    fontSize: 14,
    color: '#808080',
    textAlign: 'center',
  },
})
